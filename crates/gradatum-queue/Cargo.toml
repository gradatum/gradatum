[package]
name = "gradatum-queue"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "SQLite-backed jobs queue with atomic UPDATE...RETURNING leases"
homepage.workspace = true
keywords.workspace = true
categories.workspace = true

[dependencies]
gradatum-core      = { path = "../gradatum-core", version = "1.0.0" }
# gradatum-db-sqlite is publishable (no `publish` key); this dependency does not block
# publication of gradatum-queue.
gradatum-db-sqlite = { path = "../gradatum-db-sqlite", version = "1.0.0" }
# P2.0b dependencies (sqlx-based async Queue trait + SqliteQueue)
async-trait   = { workspace = true }
sqlx          = { workspace = true, features = ["sqlite", "runtime-tokio", "macros"] }
bincode       = { workspace = true }
chrono        = { workspace = true }
serde         = { workspace = true }
serde_json    = { workspace = true }
thiserror     = { workspace = true }
tokio         = { workspace = true }
tracing       = { workspace = true }
ulid          = { workspace = true }
# Phase 1 legacy (rusqlite-based LegacyQueue struct)
rusqlite      = { workspace = true }

[dev-dependencies]
tempfile      = { workspace = true }
tokio         = { workspace = true, features = ["full", "test-util"] }
