//! Stubs — fichier conservé pour historique ; tous les stubs ont été supprimés.
//!
//! Historique des suppressions :
//! - `NoopQueue` supprimé en T1 P2.0c : `AppState.queue` = `SqliteQueue`.
//! - `VaultRegistryStub` supprimé en T2 P2.0c : `AppState.vault` = `Arc<dyn Registry>`.
//! - `SearchEngineStub` supprimé en T3 P2.0c : `AppState.search` = `Arc<SqliteIndex>`.
//!
//! Ce fichier est vide — conservé pour l'historique git et pour faciliter les
//! références dans les messages de commit P2.0c.
