# Compte systeme dedié au daemon gradatum (UID/GID statique 985).
# Cree par systemd-sysusers lors de l'installation du paquet.
# /var/lib/gradatum : home directory (config + clés + SQLite + states)
# /sbin/nologin      : shell interactif refuse (daemon only)
#
# UID/GID 985 : validated as available on the reference deployment host (range 980-999 scanned).
# Retained for stability — margin below timesync 990.
# Validate with `systemd-sysusers --check` before tagging any release.
u gradatum 985:985 "Gradatum service" /var/lib/gradatum /sbin/nologin
m gradatum gradatum
